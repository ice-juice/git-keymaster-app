# 更新日志

面向用户的版本变更记录。发版时 CI 只抽取当前版本下的三份清单，再拼上 `docs/release-download-guide.md` 的下载建议，写入 GitHub Release 与应用内更新说明。

允许出现在说明里的标题只有：

- `### 新增功能`
- `### 优化功能`
- `### 修复问题`
- `### 下载建议`（写在下载建议文件里，不要写进本文件）

章节标题必须与即将打的标签对齐：`vX.Y.Z` 对应 `## [X.Y.Z]`。其他导语、设计说明、commit 记录都不会进入 notes。

## [Unreleased]

## [1.7.1] - 2026-09-15

### 新增功能
- **更新清单签名**：新版本会给 `latest.json` 加上与安装包同一把钥匙的签名，避免只改版本号和下载地址就能骗过检查更新

### 优化功能
- **空闲自动锁定**：离开超过设定时间后会真正锁上保险库，不再只是设置项
- **休眠与锁屏锁定**：电脑休眠，或 Windows 锁屏时立即锁定；锁定或退出时清空剪贴板
- **云同步密钥**：Secret Key 和代理密码不再明文留在本机配置里，锁定后从内存清掉
- **导出二次确认**：导出备份或云配置前要再输入一次访问密码

### 修复问题
- **检查更新误报最新**：远端清单被安卓条目盖掉桌面平台后，Windows 会显示「已是最新」；发版改为按平台合并，残缺清单不会再覆盖完整清单
- **Windows 更新签名**：安装包旁的 `.sig` 会随包上传，避免有安装包却无法校验自动更新
- **安卓覆盖安装**：安装前校验 APK 签名；拒绝装回比本机已验证版本更旧的包
- **SSH 配置**：写入托管配置前拒绝换行和控制字符，避免被注入额外命令

## [1.7.0] - 2026-09-15

### 新增功能
- **文件保险库**：加密存放证件扫描件、恢复码、合同和任意附件，可分组、备注，查看或导出需二次验证
- **备忘录**：用 Markdown 写私密图文，图片随笔记加密，支持标签、置顶和分组
- **备忘录导出**：可导出为 Markdown 或 PDF，支持单篇另存为和批量导出到文件夹

### 优化功能
- **备忘录编辑**：工具栏过窄时自动换行；可手动保存，也可在离开正文编辑区时自动保存
- **备忘录多选**：平时不显示勾选框；电脑点「多选」或右键进入，手机长按进入；选中后右下角打勾
- **置顶与标签**：置顶只看右上角按钮状态；列表里每个标签用不同底色区分

### 修复问题
- **备忘录工具栏挤叠**：窄宽度下格式按钮不再和视图切换、保存挤在同一行
- **多选占位**：列表不再常驻勾选框，避免卡片左侧留白
- **附件原名**：从带反斜杠的路径取文件名时，在 macOS / Linux 上也只留下文件名

## [1.6.0] - 2026-09-15

### 新增功能
- **界面语言**：设置里可即时切换中文、英文或跟随系统，不必重装或重启
- **统一安装包**：每个系统只下一份安装包，安装向导可选语言，不再分中文版和英文版
- **手机应用内更新**：安卓可在设置里检查、下载并用系统安装器覆盖安装（需允许安装未知应用）

### 优化功能
- **开始菜单名称**：中文系统默认显示「御钥师」，英文系统显示 Git Keymaster；卸载项始终是同一个程序
- **旧版更新通道**：以前分中英文的自动更新，现在都会升到同一份新包
- **标题栏品牌**：左上角和手机顶栏跟界面语言走，不再读运行时程序名

### 修复问题
- **中文版误下英文包**：统一更新清单后，中文用户不会再更新到英文安装包，也不会并排出两个程序
- **二维码扫描品牌名**：扫描页不再写死中文应用名，英文界面会跟着变

## [1.5.1] - 2026-09-14

### 新增功能
- 本版本以修复为主，没有新的功能入口

### 优化功能
- 本版本以修复为主，没有单独的体验优化

### 修复问题
- **手机初始化**：不再调用电脑上的文件夹选择器，保险库直接建在应用沙箱，可以进入设密码
- **手机冷启动**：沙箱里已有保险库时直接进入解锁，不再卡在初始化向导
- **中文版自动更新**：不再误下英文安装包，也不会在系统里并排出两个程序
- **更新后密码/验证码误报丢失**：列表只在确认没有机密时才标丢失，不再整页红字
- **机密落盘**：写入过程中被安装器打断时，尽量从备份恢复，避免空机密盖掉云端

## [1.5.0] - 2026-09-14

### 新增功能
- **安卓安装包**：GitHub Release 提供签名 APK，可在 64 位 ARM 手机上侧载
- **手机指纹解锁**：安卓可用系统指纹解锁；设置里可在指纹与访问密码之间切换
- **分享配置二维码**：电脑同步页出示云存储配置二维码，手机同步页或初始化向导扫码写入；不含恢复密钥，也不会自动推送数据
- **初始化扫码接入云**：手机从云端恢复时可扫描电脑上的配置码，跳过手填云存储，仍需输入恢复密钥才能解开

### 优化功能
- **同步配置操作**：手机上保存、测试单独成行，扫码放到卡片标题旁，导入导出改成轻量工具项
- **移动端界面**：顶栏和底栏跟内容区同一套配色，页面改成单列紧凑布局；身份页去掉手机上无效的 Agent / SSH 体检
- **底部菜单**：验证码改为 2FA；账号改为账密
- **解锁页居中**：手机解锁页内容在可视区域内垂直居中
- **2FA 扫码与列表**：手机 2FA 用相机扫码导入，只保留列表，顶部改成紧凑工具条
- **账密添加表单**：手机添加账密弹窗改为全宽单列、大触摸目标
- **前端按桌面/移动拆分视图**：共用页两端各自渲染，改一端不会误伤另一端界面
- **2FA / 隐私账号删除确认**：删除前弹出应用内二次确认，不再依赖系统 confirm（手机上经常弹不出来）
- **隐私账号同平台对齐**：同一平台（含大小写、github.com 这类别名）共用一份平台名和图标，新增或改图标会同步到同组账号
- **平台选择组件重构**：废除原生的尖角 datalist 下拉气泡，改用带图标与已有标识的精致卡片下拉浮窗

### 修复问题
- **手机复制验证码 / 密码**：走系统剪贴板，成功后底部轻提示，不再点了没反应
- **云同步推送卡住**：推送、拉取和状态探测不再在异步线程里跑 blocking HTTP，避免 Tokio 崩溃后界面一直转圈
- **2FA / 账密状态灯**：列表会正确标出「种子已丢失 / 密码已丢失」，不再先要访问密码再报错
- **手机冷启动认库**：沙箱里已有保险库时直接进入解锁，不再回到初始化向导；点「创建」遇到已存在的库也会进入解锁
- **手机导入云存储配置**：不再因系统选文件路径读不到而报「No such file or directory」

## [1.4.0] - 2026-09-12

### 新增功能
- **窄屏布局**：窗口变窄或在手机上打开时，改用底部导航和精简顶栏，主要页面仍能用
- **跨设备保险库档位**：新建保险库默认按手机也能解开的强度标定，换机后不必重新建库

### 优化功能
- **过重保险库提示**：访问密码派生太重时直接说明原因，不再卡死或被系统杀掉
- **过重保险库降参**：桌面端可以把过重的密码信封降到手机能用的档位，再同步出去

### 修复问题
- **左上角品牌名**：中文界面左上角固定显示「御钥师」，不再变成 Git Keymaster；发版检查会拦住这类错误

## [1.3.3] - 2026-09-11

### 新增功能
- 本版本以修复为主，没有新的功能入口

### 优化功能
- 本版本以修复为主，没有单独的体验优化

### 修复问题
- **macOS 加载密钥**：苹果自带 `ssh-add` 不再依赖 stdin 的 `-`；改为解密后写入 0600 临时文件再加载，并禁止 GUI 下挂起等口令。加载全部失败时会报出原因
- **中文版品牌名**：中文安装包左上角、macOS 菜单栏短名恢复为「御钥师」，不再显示 Git Keymaster / GitKeymaster

## [1.3.2] - 2026-09-11

### 新增功能
- **云存储小白引导**：云端同步与换机恢复提供 Cloudflare R2、AWS S3、MinIO 及其他 S3 兼容盘的逐步说明，教你如何建桶、拿密钥并填回本软件

### 优化功能
- **Agent托管**：侧栏与页面标题由「Agent」改为「Agent托管」
- **Agent 检测**：进入页面沿用上次结果，不再每次重测；需要时再点「重新检测」。macOS 不再跑用不上的 `git config`，`ssh-add` 也只查一次
- **Windows 安装目录**：NSIS 向导新装默认 `%LOCALAPPDATA%\GitKeymaster`；开始菜单和窗口标题仍是「御钥师」
- **工作空间路径**：含 `<>"|?*`、错误冒号或 Windows 保留名时直接拒绝；空格、非英文字符与同步盘只警告，仍可继续

### 修复问题
- **升级安装目录**：本机已有「御钥师」、`Git Keymaster` 或 `Git.Keymaster` 目录时，覆盖安装仍装回原处，避免并排出第二份
- **安装包主程序名**：打包脚本把主程序固定为 `git-keymaster`，不再误写成旧的 `git-account-manager`

## [1.3.1] - 2026-09-11

### 新增功能
- **macOS 开机自启动**：设置里打开后，登录 Mac 会自动启动御钥师

### 优化功能
- **设置错误提示**：失败时弹出对话框，不再只在页面顶部写一行红字
- **中文安装包显示名**：中文包装完后，桌面和程序坞显示「御钥师」，不再显示 Git.Keymaster

### 修复问题
- **macOS 指纹解锁**：开启指纹时不再因钥匙串缺少 entitlement 失败
- **指纹可用性**：本机未登记 Touch ID / 面容时，设置里会正确提示不可用

## [1.3.0] - 2026-09-10

### 新增功能
- **指纹 / 生物识别解锁**：Windows Hello 与 macOS Touch ID 可替代访问密码解锁工作空间；设置里开启后本机单独保存硬件信封，不进云同步
- **指纹重认证**：查看一次性验证码、隐私账号密码时可用系统生物识别代替再输访问密码（导出 TOTP 原始密钥默认仍要密码）

### 优化功能
- **解锁页**：开启指纹后优先显示指纹入口，密码与恢复密钥改为按需切换；启动和「立即锁定」后不再自动唤醒传感器
- **本机标识统一为 git-keymaster**：数据目录、SSH Include、agent 套接字与安装标识一并更名；首次启动会从旧的 `git-account-manager` 路径拷贝，旧目录保留作备份

### 修复问题
- **云端恢复 SSH**：同名 Host 不再在托管区内外各写一套，打开 SSH 页会折成每个别名一块
- **Windows Hello 窗口**：指纹验证弹窗提到桌面前台并挂到主窗口，避免只在任务栏闪一个按钮
- **进程稳定性**：配置锁被先前异常污染后，设置页读代理等操作不再把整个窗口打死

## [1.2.1] - 2026-09-10

### 新增功能
- 本版本以修复与安装包命名为主，没有新的功能入口

### 优化功能
- **安装包命名**：GitHub Release 里中英文成对出现，例如 `Git.Keymaster_1.2.1_x64_zh-CN.msi` 与 `Git.Keymaster_1.2.1_x64_en-US.msi`；文件名末尾 `zh-CN` 是中文界面，`en-US` 是英文界面

### 修复问题
- **换机恢复**：Windows 写入本机 SSH 配置被拒绝时不再整段中断云端拉取；工作空间 SSH 正本仍会落盘，并按身份补齐 Host
- **云同步**：本机 SSH 配置仍为空时，不会用空文件覆盖云端已有 Host
- **macOS 卡顿**：切页不再卡住整窗；解锁后的慢活也不再锁住界面

## [1.2.0] - 2026-09-10

### 新增功能
- **2FA / TOTP**：加密保管一次性验证密钥，支持手动/URI/图片/屏幕扫码导入、按需查看验证码、取回原始密钥与二维码
- **隐私账号**：按平台聚合多账号、分组检索、密码二次验证、历史版本回滚、可联动 TOTP
- **自定义图标**：内置平台图标库，上传图片后自动居中裁剪并压缩为 128×128 WebP
- **解锁开门动画**：赛博全息 / 经典金匙 / 极客量子三套风格，设置页可切换并即时预览
- **网络代理**：可配置 HTTP/HTTPS/SOCKS5，检查更新、GitHub API、Git HTTPS、SSH 与云同步走同一条代理
- **环境自查**：设置页可检查本机安全环境
- **单实例保护**：再启动第二个窗口时，会先通知已运行实例锁定保险库再退出

### 优化功能
- **品牌与窗口**：应用更名为御钥师，使用无边框标题栏与新 logo
- **云同步页**：进入界面不再探测云端，沿用启动拉取与定时/编辑同步的结果；需要时再手动刷新
- **剪贴板**：复制凭据走系统剪贴板并按设置秒数自动清空，避免浏览器权限弹窗

### 修复问题
- **云同步**：TOTP/账号变更会纳入自动推送与状态比对；历史快照恢复同时回滚验证密钥与账号密码
- **多端合并**：账号改密以较新条目为准，避免旧密码盖掉新密码
- **OpenSSH 探测**：macOS / Linux 改用本机或 Homebrew ssh，不再误报需要 Git for Windows
- **Linux 屏幕扫码**：安装包不再编入系统截屏库（与 Ubuntu 22.04 不兼容），请改用图片或 otpauth URI 导入

## [1.1.1] - 2026-09-09

### 新增功能
- **应用更新**：支持从 GitHub Release 检查、下载并安装新版本，可自定义更新源或改用静态清单
- **设备与云同步**：支持设备隔离、自定义云端恢复范围、S3 配置导入导出，以及一键重练重置

### 优化功能
- **设置页**：改为分栏导航，云端同步界面去掉重复操作

### 修复问题
- **仓库路径**：修复在 Unix 上仓库路径相关测试失败

## [1.1.0] - 2026-09-08

### 新增功能
- **身份管理**：多身份向导、加密工作空间、SSH 密钥生成与导入
- **SSH 与 Agent**：托管写入 `~/.ssh/config`、ssh-agent 加载/卸载、`ssh -T` 体检
- **仓库**：地址识别、归属推断、本地扫描、按身份克隆
- **云同步**：将加密工作空间备份到用户自己的 S3 / R2
- **GitHub PAT**：校验账号、拉取组织、上传公钥
